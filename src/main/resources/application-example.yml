spring:
  datasource:
    url:
    username:
    password:
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: cn.pasteme.common.entity
logging:
  level:
    cn.pasteme.backend: debug